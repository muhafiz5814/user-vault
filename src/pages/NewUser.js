// A page component to create a new user.
// It renders UserForm component as child to show input form elements.
import UserForm from '../components/UserForm';

const NewUser = () => {

    return (
        <UserForm />
    );
};

export default NewUser;